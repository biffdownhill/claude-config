# Claude Code internal runtime files — never tracked
.last-cleanup
.obsidian/
backups/
cache/
debug/
file-history/
history.jsonl
ide/
paste-cache/
plans/
plugins/
projects/
session-env/
session-logger.log
sessions/
shell-snapshots/
skills/
statsig/
statusline-command.sh
tasks/
telemetry/
todos/

# Real settings file — tracked separately per machine
settings.json
settings.json.bak

# macOS
.DS_Store

# Secrets — never commit these
*.key
*.pem
*.p12
*.pfx
*.cert
*.crt
.env
.env.*
*_secret*
*_token*
*credentials*

# Only these paths are tracked:
# CLAUDE.md
# agents/
# templates/
# settings.template.json
# bootstrap.sh
# .gitignore
