---
date: {{date}}
tags: [gotcha]
status: active
areas: []          # coarse topic buckets, lowercase — e.g. [supabase, rls]. Drives ambient recall.
files: []          # globs this note applies to — e.g. ["server/**/delete*.ts"]. Enables edit-time recall.
symptoms: []       # observable symptom phrases — e.g. ["DELETE returns 200", "0 rows removed"].
---

## What surprised us
<!-- The unexpected behaviour, in one or two sentences. -->

## Why it works that way
<!-- The actual mechanism — library quirk, language semantics, environment config, etc. -->

## What to do about it
<!-- Workaround, convention, or "just be aware". Include code snippet if useful. -->
